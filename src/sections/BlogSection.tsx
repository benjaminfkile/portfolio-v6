import type { SectionProps } from './types';

/**
 * Placeholder for the live `blog` section (spec §3.5) — the N most recent
 * published posts as teaser cards, fetched at runtime from `GET /api/posts` so
 * post publishing stays decoupled from page publishing. Registered now; the
 * runtime listing lands in the next task. Renders nothing for the moment.
 */
export default function BlogSection(_props: SectionProps) {
  return null;
}
